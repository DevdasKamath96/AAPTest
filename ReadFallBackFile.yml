---
 - name: Reading a fallback file
   hosts: localhost
   tasks:
     - name: read the fallback file
       slurp:
         src: /etc/passwd
       register: pass_content

     - name: display sshd_config data
       debug:
          msg: "{{pass_conten.content | b64decode }}"
